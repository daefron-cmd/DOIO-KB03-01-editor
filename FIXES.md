2026-08-10 | DOIO-20260810-HID-REOPEN | installed app stopped receiving outer-ring scroll input after a raw-HID open failure | an orphaned duplicate app process held the single-owner HID interface, then `OSError` from the new process's opener escaped `maybe_reopen` and terminated its I/O thread instead of entering the documented retry path | terminate the stale owner and treat HID open errors as transient unavailable-device results using the existing reconnect backoff | process audit showed duplicate `main.py` owners; failing regression `test_open_error_is_retried_instead_of_escaping_worker_loop`; 127 tests passed after correction
2026-08-10 | DOIO-20260810-ALL-LAYER-FIRMWARE | outer ring was dead on layer 0 and used ordinary firmware actions on layers 1–3 instead of the documented cross-layer MX Master emulation | the stored candidate firmware was built before the all-layer encoder-map and raw-HID implementation, so the deployed artifact could not satisfy the cross-layer invariant | build, retain, and flash a new firmware image from the current source | old artifact timestamp 10:41 preceded source changes at 18:22–18:35; QMK build passed; ELF exports `inertia_host_ready`, `inertia_send_scroll_ping`, `keyboard_post_init_user`, and `dynamic_keymap_reset`; SHA-256 `34f31dd85f5aa81427d5b373c74d7512b6a2616297722ce5684ae1eccbe539a0`; `dfu-util` downloaded 40164 bytes to `1EAF:0003` alt 2 with no device-status error; runtime returned as `D010:0301`; live VIA readback reports `USER(0)`/`USER(1)` for outer encoder on layers 0–3; user confirmed ordinary vertical fallback scrolling on all layers with the GUI closed
2026-08-10 | AX-TAKEOVER-20260810 | MX Master takeover stayed in firmware fallback while Accessibility was enabled for DOIO KB03-01.app | the signed launcher replaced itself with uv, which spawned an unsigned virtual-environment Python process; that Python process called AXIsProcessTrusted and did not inherit the bundle's TCC identity | embed the virtual-environment interpreter in the signed DOIOKB03 executable so the trusted bundle process owns the GUI and Quartz calls | before: live process tree contained only uv and .venv/bin/python3 and the GUI reported Raw ok / Quartz ok / AX no / Host waiting; after: installed bundle has one DOIOKB03 process with no uv/Python child, codesign verification passed, launcher regression passed, and 128 tests passed with QT_QPA_PLATFORM=offscreen; final AX/takeover confirmation requires re-granting the newly signed bundle in System Settings
2026-09-18 | HID-QUEUE-20260918 | reported disconnected-device memory growth and stale-command replay | timeout cancellation removed futures but retained their queued writes; racing write errors completed cancelled futures | remove cancelled unsent frames, drop/coalesce heartbeats, guard future completion, and close running handles on their IO thread | before: 100 cancellations retained and replayed 100 writes and a cancellation/write race raised InvalidStateError; after: queue, replay, offline heartbeat, and cancellation-race regressions pass in tests/test_hid_io.py
2026-09-18 | VIA-REPLIES-20260918 | late replies violated request/response data integrity | command-ID-only matching attributed timed-out replies to newer requests | reset sent timeouts, synchronize each open with a VIA protocol-version barrier, and validate echoed request addresses/values | before: column 1 reply completed column 2 read; after: timeout, wrong-echo, buffered-reply, and delayed-after-quiet-read regressions pass in tests/test_hid_io.py
2026-09-18 | LIGHTING-SAVE-20260918 | UI reported lighting as persisted when the saved state differed | save could overtake coalesced slider writes and acknowledgement captured later edits; revert retained pending values | flush before save, retain the immutable save target, and discard pending values on revert/readback | before: save emitted before brightness write and ACK marked a later value saved; after: three lighting persistence regressions pass in tests/test_ui_state.py
